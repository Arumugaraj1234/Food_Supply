enum AppUpdateType { noUpdate, optionalUpdate, mustUpdate }
enum InitialScreen { appInitScreen, loginScreen, homeScreen }
enum PaymentStatus { paid, unpaid }
enum OrderStatus {
  dishPrepared,
  pickedAtHotel,
  delivered,
  completed,
  cancelled
}
